import { ProducFilterPipe } from './produc-filter-pipe';

describe('ProducFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ProducFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
